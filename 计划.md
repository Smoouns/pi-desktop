# Codex 总控提示词：将 Pi Desktop 改造成本地小说创作 Agent

你现在位于 `gustavonline/pi-desktop` 的 fork 仓库中。

我要把这个项目改造成一个**本地优先（local-first）的长篇小说 AI 创作桌面应用**。

这不是 coding agent，也不是要重新实现一个 agent harness。

核心原则是：

**保留 Pi Desktop 已经实现的通用桌面基础设施和 Pi runtime 集成，在其上增加小说领域 UI、小说 tools 和 context engineering。**

---

## 0. 工作方式

你拥有代码修改权限，请直接完成工作，不要只给建议。

开始修改前：

1. 阅读当前仓库：
   - `README.md`
   - `docs/ARCHITECTURE.md`
   - `docs/CAPABILITY_MODEL.md`
   - `docs/PACKAGES.md`
   - 与 RPC、extension UI、workspace、file panel、session、chat 有关的核心代码。

2. 理解并尊重现有三层架构：
   - Desktop Host
   - Pi Runtime
   - Packages / Extensions

3. 不要 fork、复制或重新实现 Pi runtime。

4. 不要进行与目标无关的大规模重构。

5. 优先复用现有组件、状态管理、文件操作、RPC、session、streaming、model/provider 和 extension infrastructure。

6. 对 coding-specific 功能：
   - 优先隐藏、降级到 Advanced，而不是直接删除；
   - 除非删除明显不会影响底层架构，否则保留现有实现。

7. 每完成一个阶段：
   - 运行现有 typecheck / lint / test / build；
   - 修复由本阶段引入的问题；
   - 保持应用能够运行。

不要因为某个细节存在多种合理实现就停下来询问我。选择与当前代码架构最一致、侵入性最低的实现。

---

# 1. 产品目标

最终应用应该是一个专注长篇小说的 AI Desktop Workspace。

主要布局目标：

```text
┌───────────────────────────────────────────────────────────┐
│ Novel Project                                             │
├──────────────┬────────────────────────┬───────────────────┤
│ Novel        │ Document Editor        │ AI Assistant      │
│ Navigator    │                        │                   │
│              │ Chapter 47             │ Pi conversation   │
│ Characters   │                        │                   │
│ World        │ Novel text...          │                   │
│ Outline      │                        │                   │
│ Chapters     │                        │                   │
│ Notes        │                        │                   │
│ Memory       │                        │                   │
├──────────────┴────────────────────────┴───────────────────┤
│ Context Inspector                                         │
└───────────────────────────────────────────────────────────┘
```

但不要在第一步就重写整个 layout。

渐进式改造。

---

# 2. 架构原则

新增功能尽量分成两个领域：

```text
Desktop Novel UI
+
Novel Pi Extension / Domain Layer
```

Desktop Novel UI 负责：

- Novel Navigator
- Document Editor
- Context Inspector
- 当前打开文档
- 当前选择的人物/设定
- pin/unpin context
- UI 状态

Novel extension/domain layer 负责：

- 小说文件语义
- context selection
- novel tools
- search
- workflow
- memory policy

不要让 Desktop UI 自己承担 agent reasoning。

不要把小说业务逻辑散落在现有 generic desktop core 中。

尽量建立清晰模块，例如：

```text
src/novel/
    components/
    state/
    project/
    context/
```

实际目录应根据当前仓库结构调整，不要机械创建不符合现有架构的目录。

---

# 3. 小说项目格式

实现一种简单、local-first、human-readable 的小说项目。

项目根目录建议：

```text
my-novel/
│
├── .novel/
│   └── project.json
│
├── manuscript/
│   ├── chapter-001.md
│   ├── chapter-002.md
│   └── ...
│
├── characters/
│   ├── character-a.md
│   └── character-b.md
│
├── world/
│
├── outline/
│   ├── master.md
│   └── volume-01.md
│
├── notes/
│
└── memory/
    ├── timeline.md
    ├── plot-threads.md
    └── summaries/
```

要求：

- 文本资料首先使用 Markdown。
- `.novel/project.json` 只保存必要项目元数据。
- 第一版不要引入数据库。
- 第一版不要引入 vector database。
- 所有用户创作内容保持普通文件，可脱离本软件直接读取和编辑。
- 不要建立 proprietary binary format。

如果打开的普通目录没有 `.novel/project.json`：

提供非破坏性的：

`Initialize as Novel Project`

操作。

初始化只创建缺失的标准目录和元数据，不移动、不覆盖用户已有文件。

---

# 4. Phase 0：建立改造边界

首先创建：

`docs/NOVEL_ARCHITECTURE.md`

内容包括：

- 当前 Pi Desktop 架构
- 哪些 generic components 保留
- 哪些 coding-centric UI 默认隐藏
- 新增 Novel UI 的边界
- Novel extension/domain layer 边界
- 文件数据模型
- Context Engine 设计
- 后续 Memory 设计
- upstream compatibility strategy

原则：

**尽量让未来还能从原始 pi-desktop upstream 合并更新。**

避免无意义地格式化或重写大量 upstream 文件。

---

# 5. Phase 1：Novel Project + Novel Navigator

识别 Novel Project 后，在 sidebar 中加入小说语义导航。

Navigator 至少包含：

- Manuscript / Chapters
- Characters
- World
- Outline
- Notes
- Memory

这些 category 根据项目目录实时读取。

要求：

- 点击文档能使用已有 file infrastructure 打开；
- 支持创建新的 Markdown 文档；
- 支持基本 rename；
- 支持删除，但必须 confirm；
- category 不存在时允许创建；
- filesystem 是 source of truth；
- 不维护重复的文件数据库。

保留现有 workspace/session 基础设施。

不要为了 Novel Navigator 重写整个 workspace system。

---

# 6. Phase 2：Document Editor

将现有 file viewing 能力扩展成适合小说写作的文本编辑能力。

第一版只需要：

- Markdown/plain text editing
- Save
- keyboard shortcut save
- dirty indicator
- autosave（合理 debounce）
- 当前文档状态
- 外部文件变化检测或至少安全处理
- 防止未保存内容被静默覆盖

不要第一版开发复杂富文本编辑器。

不要开发类似 Word 的排版系统。

优先使用当前项目已有能力或成熟的轻量 editor component。

目标是：

**稳定写文本，而不是做排版软件。**

首先允许现有 UI 布局继续工作。

如果需要，先让 editable document panel 与 chat 并存。

完成稳定编辑之后，再逐步向三栏：

```text
Novel Navigator | Editor | AI Chat
```

演进。

不要为了视觉目标一次性重写全局 layout。

---

# 7. Phase 3：Novel Extension / Novel Tools

为 Pi 增加小说领域 tools。

至少实现等价能力：

```text
list_story_files()

read_story_document(path)

read_character(name)

read_chapter(identifier)

read_outline(scope)

search_story(query)

get_current_document()

get_adjacent_chapters()
```

命名可以根据 Pi extension conventions 调整。

要求：

- 使用 Pi extension/package 机制；
- 不修改 Pi runtime；
- tool path 必须限制在当前 Novel Project；
- 防止 path traversal；
- 默认读操作优先；
- 写正文仍通过应用自己的 editor/save flow；
- 不允许 agent 无提示修改 Canon 文档。

`search_story` 第一版使用简单可靠的方法：

- filename
- text search
- headings
- optional tags/frontmatter

不要加入 embedding。

---

# 8. Context Engine MVP

这是整个项目最重要的部分。

创建独立的 Context Builder。

对于一次 AI 请求，Context Builder 第一版采用 deterministic policy。

优先级建议：

```text
1. 当前正在编辑的文档
2. 用户手动 pinned documents
3. 用户明确选中的人物/世界观条目
4. 当前章节的相邻章节
5. prompt 中明确出现的人物名
6. prompt 中明确出现的世界观实体
7. relevant outline
8. style guide（存在时）
```

不要试图把整个小说放进 context。

不要第一版让 LLM 自己完全决定需要什么 context。

Context Builder 必须产生一个可检查的数据结构，例如：

```text
ContextItem {
    path
    category
    reason
    estimatedTokens
    priority
    pinned
}
```

具体类型根据当前语言和代码风格实现。

---

# 9. Context Inspector UI

增加 Context Inspector。

用户至少应该看到：

```text
Context

✓ manuscript/chapter-046.md
  reason: adjacent chapter
  ~6200 tokens

✓ characters/lin-mo.md
  reason: mentioned entity
  ~1100 tokens

✓ world/ruins.md
  reason: mentioned entity
  ~1700 tokens

--------------------------------
Estimated total: ~9000 tokens
```

用户能够：

- Pin
- Unpin
- Remove from this request
- Add another document

Context Inspector 的核心产品原则：

**用户应该知道模型为什么知道一件事。**

不要把 context selection 做成完全不可见的黑箱。

token count 第一版允许估算，不要求 tokenizer 完全精确。

---

# 10. AI Chat 与当前文档联动

保持现有 Pi streaming/session/chat infrastructure。

增加：

- 当前 active document
- 当前 selected entities
- 当前 context set

发送用户消息时：

Novel Context Builder 生成本轮 context。

不要永久污染 system prompt。

不要把所有项目文件永久注入 session。

明确区分：

```text
conversation history
novel project context
current request
```

如果当前 Pi extension API 有更符合 upstream 设计的 context injection 方法，优先使用它。

---

# 11. Canon 安全原则

第一版就建立以下约束：

这些目录视为 author-controlled Canon：

```text
characters/
world/
outline/
```

Agent：

- 可以读取；
- 可以提出修改建议；
- 不得无提示直接修改。

后续可以增加：

```text
propose_canon_change()
```

但此阶段不需要完整实现。

任何 agent-driven Canon mutation 都必须经过用户确认。

---

# 12. Memory：只设计，不在第一轮完整实现

在 `docs/NOVEL_ARCHITECTURE.md` 中预留四层 memory：

```text
CANON
Author-confirmed facts.

DERIVED
State inferred from manuscript.

SUMMARY
Generated chapter/scene summaries.

SCRATCH
Disposable agent working memory.
```

重要原则：

Canonical facts 和 AI-generated summaries 不能混为一谈。

生成内容必须能够追踪 source/provenance。

第一轮不要实现复杂自动 memory pipeline。

---

# 13. 后续 Workflow 架构

为以后预留以下模式，但第一轮不要实现复杂 multi-agent：

```text
Write
Rewrite
Brainstorm
Plan
Check Continuity
Summarize
```

这些模式未来应该主要通过：

- prompt
- skills
- extension commands
- context policy

实现。

不要为了这些模式创建多个 autonomous agents。

---

# 14. 第一轮明确不要做

不要：

- 重写 Pi runtime
- fork Pi agent loop
- 引入 LangChain
- 引入 LangGraph
- 引入新的 agent framework
- 引入 multi-agent orchestration
- 引入 vector database
- 引入 embedding pipeline
- 引入大型数据库
- 引入 CRDT
- 引入 cloud backend
- 引入 account system
- 引入 sync server
- 做复杂富文本编辑器
- 做可视化人物关系图
- 做可视化 timeline
- 做自动伏笔系统
- 做自动 Canon mutation
- 大规模删除 upstream coding capabilities
- 为了“代码更干净”而重写已经正常工作的 Pi Desktop infrastructure

这些以后根据真实 failure mode 决定。

---

# 15. Coding-specific UI 处理策略

Terminal、coding-specific commands 等现有能力不要急着删除。

优先：

```text
default hidden
或
Advanced mode
```

保留底层能力有利于：

- 调试
- upstream merge
- 高级用户
- extension 开发

Novel Mode 应该是默认体验。

---

# 16. Upstream Compatibility

这是重要要求。

改造时：

- 尽量新增文件，而不是侵入式修改旧文件；
- domain code 集中；
- 不改变 Pi RPC protocol；
- 不重新实现 Pi session；
- 不重新实现 provider/model auth；
- 不复制 upstream agent代码；
- 不随意改变 generic filesystem abstraction；
- 不做全仓库格式化；
- 不做与小说无关的 dependency migration。

在 `docs/NOVEL_ARCHITECTURE.md` 中记录：

```text
Upstream-sensitive files
Novel-owned files
Intentional upstream modifications
```

---

# 17. Acceptance Criteria

第一轮工作完成后，我应该能够：

1. 启动 Desktop 应用。

2. 打开一个普通本地目录。

3. 将其 Initialize 为 Novel Project。

4. 在左侧看到：
   - Chapters
   - Characters
   - World
   - Outline
   - Notes
   - Memory

5. 创建并编辑 Markdown 小说文件。

6. 保存后文件直接存在磁盘。

7. 打开一个章节。

8. 在 AI Chat 输入：

   “根据当前章节、林默的人物设定和遗迹设定，继续设计这一场对话。”

9. 系统能够构造 novel context。

10. Context Inspector 能看到被加入的文件以及原因。

11. 用户可以 pin/unpin context。

12. Pi 正常 streaming 回复。

13. 原有 Pi session/model/provider 基础能力仍然工作。

14. Agent 不会自动修改 characters/world/outline。

15. typecheck/lint/tests/build 不因本次改造而失败。

---

# 18. 实施顺序

严格按照：

```text
inspect repository
      ↓
docs/NOVEL_ARCHITECTURE.md
      ↓
Novel Project detection
      ↓
Novel Navigator
      ↓
Document editing
      ↓
Novel extension/tools
      ↓
Context Builder
      ↓
Context Inspector
      ↓
Chat integration
      ↓
tests
      ↓
polish
```

不要从 Memory、多 Agent、RAG 开始。

---

# 19. 测试要求

至少增加针对以下逻辑的测试：

- Novel Project detection
- project initialization 不覆盖已有文件
- path traversal protection
- category scanning
- Context Builder priority
- pinned context
- entity matching
- context token budget behavior
- Canon write guard

如果现有项目有 UI/E2E 测试体系，对关键 happy path 增加最小 E2E coverage。

不要为了测试引入全新的大型 test framework。

---

# 20. 最终交付

完成第一轮实现后：

更新：

```text
docs/NOVEL_ARCHITECTURE.md
README 或单独 NOVEL_README.md
```

并给出简洁总结：

```text
Implemented
Architecture decisions
Files/modules added
Existing modules modified
Tests run
Known limitations
Recommended Phase 4
```

不要只描述代码。

直接完成可运行实现。

最终目标不是打造“拥有最多功能的 AI 小说软件”。

目标是：

**建立一个简单、透明、local-first、可长期演进的小说创作环境，其中 Pi 负责 agent runtime，而小说 Context Engine 才是领域核心。**

---

# 已确认的第一轮决策（2026-08-13）

## 第一轮范围

第一轮采用“先做稳定编辑、透明上下文和阅读导航”的路线，不实现完整创作工作流状态机。

第一轮实现：

- Novel Project 检测与非破坏性初始化。
- 小说语义导航、Markdown/plain-text 编辑、保存、dirty 状态与安全处理。
- Novel 文件读取、搜索和当前文档/相邻章节识别。
- deterministic Context Builder、Context Inspector、pin/unpin 与本轮移除。
- 当前文档、选中实体、当前 novel context 与现有 Pi streaming/session/chat 联动。
- Canon 读取保护：Agent 可以读取和提出建议，但不得无提示修改 Canon。

第一轮暂不实现：

- “章节卡 -> 候选正文 -> 验证 -> 人工接受 -> Canon 晋升”的工作流状态机。
- 自动更新连续性账本、current-story-state、progress 或 Canon index。
- 自动 Canon mutation、复杂 memory pipeline、多 Agent 编排、RAG/embedding/vector database。

章节卡、事件纲要、评审、连续性提案和审批状态第一轮继续作为普通 Markdown 管理。应用只读取并展示显式 `status:`，不维护另一份隐式内容数据库。

## 项目格式：内容类型 + 生命周期/权威性

文件同时具有两条独立属性：

内容类型：`manuscript`、`canon`、`planning`、`draft`、`craft`、`note`、`research`、`memory`、`archive`、`asset`、`export`。

生命周期/权威性：`canonical`、`approved`、`proposed`、`draft`、`historical`、`derived`、`reference`、`external-reference`。

不能只根据目录名判断权威性。正式正文可以位于历史项目的 `drafts/` 下；Canonical Text Index、`.novel/project.json` 的 authority 映射和文件显式状态优先于目录推断。

新项目默认预留：

```text
my-novel/
├── .novel/
├── manuscript/   # 当前正式正文
├── canon/        # 作者确认的设定、人物、世界观、连续性
├── planning/     # 事件纲要、章节卡、评审、提案、进度
├── drafts/       # 候选稿和模型比较稿
├── craft/        # 风格、写作规范、交接说明、模板
├── notes/        # 灵感与未确认问题
├── memory/       # 派生摘要与临时工作记忆
├── research/     # 有来源的现实资料和考据
├── assets/       # 地图、角色图、封面等二进制素材
├── archive/      # 明确废止但需保留的历史资料
└── exports/      # EPUB、DOCX、发布稿等派生产物
```

第一轮只需创建必要目录；`research`、`assets`、`archive`、`exports` 作为可扩展保留项。`memory` 预留四层：`CANON`、`DERIVED`、`SUMMARY`、`SCRATCH`，但第一轮只设计，不自动生成复杂摘要。

## 既有项目导入与测试样本

对文件杂乱的既有项目采用“迁移预览 -> 用户确认 -> 独立复制样本”，不修改原项目结构、不移动文件、不改写 Markdown 内容。

已确认的测试源项目：`D:\PycharmProjects\novel_test\fate-control-cycle`。

复制样本：`fixtures/novel-projects/fate-control-cycle-sample/`。

导入必须生成：

- `.novel/project.json`：目录映射、authority 入口和格式版本。
- `.novel/import-manifest.json`：原路径、目标路径、SHA-256、内容类型、初始权威性、判定理由、源 `status:`。
- `.novel/import-report.md`：导入边界和人工可读报告。

当前样本的关键判定：

- `drafts/earth-volume-1/chapters/001.md` 至 `016.md` 复制到 `manuscript/`，状态为 `canonical`。
- `017.md` 与其 continuity proposal 复制到 `drafts/` / `planning/`，状态为 `proposed`，不得当作正史。
- `controlled-trial`、模型比较和早期重写稿保留为 `historical` draft，默认不进入上下文。
- 明确 `RETIRED` / `SUPERSEDED` 的规划文件复制到 `archive/`。

导入脚本：`scripts/import-fate-control-cycle-sample.ps1`。脚本只读源目录、只写空的样本目录，并逐文件校验 SHA-256。

## 后续完整工作流预留

后续阶段可以在现有文件关系之上增加：

```text
章节卡已批准
  -> 候选正文
  -> 可选：机械验证报告（可追溯证据）
  -> 等待人工接受
  -> Canon 正文、连续性账本、当前状态和进度的一致性晋升
```

第一轮只需提供只读关系模型和显式状态读取接口；任何晋升或 Canon 修改都必须是用户确认的、可审计的操作，不能由 Context Builder 或 Agent 隐式完成。

## 调整后的实施顺序

```text
inspect repository
      ↓
docs/NOVEL_ARCHITECTURE.md
      ↓
Novel Project detection + layout/authority model
      ↓
Novel Navigator
      ↓
Document editing
      ↓
Novel extension/tools
      ↓
Context Builder
      ↓
Context Inspector
      ↓
Chat integration
      ↓
tests + polish
      ↓
后续阶段：confirmed chapter workflow + Canon promotion
```

## 当前实现状态（2026-08-13）

第一轮已完成并通过收尾验证：

- Novel Project 检测、非破坏性初始化和双层内容/权威性分类。
- Novel Navigator、Markdown 文件创建入口与现有 FileViewer 编辑保存流程联动。
- 文件扫描、相邻章节/实体匹配、确定性 Context Builder 和 token budget。
- Context Inspector 的路径、原因、权威性、估算 token、Pin/Unpin、移除和手动添加文档。
- Chat 联动；桌面端仍通过 request prompt 将可审计的上下文标记传给现有 Pi RPC，Pi Novel Tools extension 在模型调用前通过 `context` hook 清理标记并注入 transient custom message，未改变 Pi runtime/session 协议。
- `fate-control-cycle` 独立复制样本及 119 文件 SHA-256 导入校验。
- 领域 smoke 覆盖路径穿越、显式 authority、优先级与 token budget。

已执行：`npm run check`、`npm run build:frontend`、`npm run test:novel-domain`、`scripts/test-novel-import.ps1`、Rust `cargo check`、`git diff --check`。

已知限制：桌面启动依赖当前终端能找到 `cargo`；本机已用 `C:\Users\Silence\.cargo\bin\cargo.exe` 验证 Rust，`tauri dev` 已进入启动流程并监听 Vite 端口。桌面端到 RPC 仍需要携带本轮上下文标记以保持现有协议兼容；Pi extension 已在 provider 请求前清理历史标记并仅保留最新请求的 transient context，后续可再考虑让 RPC 支持独立的 request metadata 通道。

## 第二轮实现状态（2026-08-13）

已开始实现章节工作流的最小安全闭环：

- 从 `chapter-cards/`、候选正文、`continuity-proposals/`、`reviews/` 和 Canon 路径投影章节状态。
- 只有当前、显式的正文人工接受记录才允许晋升；结构化验证报告用于提供可追溯的机械检查证据，不会自动接受或晋升。报告必须在头部明确声明 `verification_status: PASS` 或 `PASS_WITH_WARNINGS`（兼容 `verification`、`status`、`result`），正文中单独出现的 “PASS” 不算通过。
- 报告如声明章节号或候选稿路径，必须与当前章节和候选正文匹配；仅由文件名绑定的旧报告仍可读取，但会提示缺少 `source_text` 绑定。
- 第一次用户操作只接受 proposal；第二次用户操作才复制正文到 Canon。
- Canon 目标已存在、路径越界或状态不足时均 fail-closed；晋升写入 `.novel/promotion-log.md`。
- 样本 Chapter 017 具有匹配候选正文的结构化 `PASS` 验证报告，但仍保持等待人工验收状态；它不会因机械通过而误显示已接受或已晋升。

本轮已完成：验证报告的结构化读取与 fail-closed 状态投影。`progress`、`current-story-state`、`continuity-ledger` 仍保持普通 Markdown、手动维护；在没有逐字段的用户确认和可审计变更预览前，不会由晋升操作自动改写。

本轮已完成：桌面端确认样本第 017 章在候选稿存在时仍保持“等待人工验收”，无论机械验证报告缺失、失败或通过，都不会替代章节卡和正文的用户确认。对可操作章节，应用使用独立的工作流确认浮层替代浏览器确认框：接受提案时预览且仅允许 `status` 的单行变化；晋升时预览候选正文、非覆盖 Canon 目标和审计记录，同时明确 `progress`、`current-story-state`、`continuity-ledger` 不会被自动改写。浮层支持取消、关闭和 Escape；真正写盘仍只发生在用户点击确认后，并复用既有 fail-closed 校验。

本轮继续完成：关联文档更新采用“显式补丁、逐项选择”而不是从自然语言事实变化中猜测修改。连续性提案可选地提供 `<!-- novel-record-updates -->` JSON 块，逐项声明 `current-state`、`continuity-ledger` 或 `progress`。支持 `append` 精确追加，或 `replace-once` 的唯一锚点替换；晋升浮层将替换项显示为 `- 原文 / + 新文`。用户可取消单项；未提供、格式错误、目标不受支持、锚点不存在/重复，或预览后文件指纹已变化的更新均 fail-closed，不会改写任何关联记录。晋升会先重新计算全部选中补丁，再复制正文；后续任一写入失败时恢复已写关联文件并删除刚创建的 Canon 目标。被选中的补丁与正文复制一起写入 `.novel/promotion-log.md`。后续若需要可逆操作，应在此结构化补丁基础上另行设计变更历史和回滚确认，而不能以全文模糊替换实现。

本轮继续完成：每次成功晋升会在 `.novel/promotion-history/<id>.json` 保存私有快照，记录新 Canon 的指纹，以及每个已选关联补丁的完整晋升前/后文本和指纹。侧栏仅对带有未回滚历史的“已晋升”章节显示“回滚本次晋升”；回滚使用独立确认浮层展示反向差异。确认前会重新核对 Canon 和所有关联记录仍完全等于晋升后的指纹，任何人工编辑、文件缺失或历史异常都会拒绝回滚而不覆盖内容。通过检查后，回滚恢复关联记录的晋升前文本、只删除该次新建的 Canon 正文、将历史标记为已回滚并写入审计日志；操作中后续写入失败时会恢复自身已经变动的文件。`USER_ACCEPTED` 不会被自动撤销，因此回滚后仍需按正常流程再次确认晋升。

本轮继续完成：通过 Pi 原生 extension 机制安装 `~/.pi/agent/extensions/pi-desktop-novel-tools.ts`，只向活动 Novel Project 暴露七个受限的只读工具：`list_story_files`、`read_story_document`、`read_chapter`、`read_character`、`read_outline`、`search_story`、`get_current_document`。所有工具均要求项目根目录存在 `.novel/project.json`，路径只能是项目内的相对路径，并限制扫描/搜索数量和读取长度。`get_current_document` 只读取当前请求中由桌面端注入的 `<novel-context>`，不保存另一份当前文件状态。扩展不提供写入、章节状态变更、Canon 晋升、关联补丁或回滚工具；已存在但不含桌面端版本标记的同名扩展被视为用户维护文件，绝不覆盖。扩展 smoke 使用 Pi 自己的加载器和复制样本验证注册、读取、搜索、当前文档和路径越界拒绝。

本轮继续完成：利用 Pi 的 `context` extension hook 将桌面端注入的 `<novel-context>` 从模型将要接收的普通用户消息中移除，并以 `display: false` 的临时 custom message 注入最新请求上下文；历史请求中的上下文块不会重复送入模型。原始会话仍保留用户请求和文件选择标记，`get_current_document` 仍从最新请求上下文解析当前文档。新增 smoke 验证历史块清理、最新块保留和 transient message 注册。

上下文传递现改为“文件索引 + Agent 按需读取”：桌面端只注入项目内相对路径、内容类型、权威性、选择理由、估算 token 与 `required` / `on-demand` 读取级别，绝不拼接任何 Markdown 正文。当前文件、固定/手动加入资料、当前状态、连续性账本和风格指南标为 `required`；相邻章节和关键词命中的资料为 `on-demand`。各 `/novel-*` 角色提示词要求 Agent 在写作、规划、评审或提出世界观提案前，先通过受限读取工具加载全部 `required` 文件；Context Inspector 同步显示“必须读取”或“按需读取”。

本轮继续完成：在 Novel Tools extension 中增加 `/novel-world`、`/novel-plan`、`/novel-write`、`/novel-review` 四个可选提示词预填充命令。命令只把带有三 Agent 边界的模板放入编辑器，不自动提交、不创建 Agent 实例、不写入任何文件；用户可先检查本次任务和上下文，再手动提交。`novel-review` 明确只响应用户提出的不满意意见，不能替代章节卡确认、正文人工验收或 Canon 晋升。

本轮工作流修订：不再把 `reviews/` 或结构化 `PASS` 当作正文验收与 Canon 晋升的强制前置。`reviews/` 是规划 Agent 在用户提出不满意后生成的返工分析/记录，允许缺失或多轮，不能假定与正文一一对应。章节卡先由用户确认，写文 Agent 才可据此生成候选正文；用户阅读正文后单独确认“正文通过人工验收”，随后才可晋升 Canon。两类确认都写入 `.novel/acceptances/` 的路径与指纹记录，章节卡或正文发生后续改动时确认自动失效。用户可在章节卡或正文阶段填写“需要修改”，应用会生成 `planning/revision-requests/` 下不可覆盖的 Markdown 交接单，规划 Agent 或写文 Agent 完成返工后回到相应的人工确认关口。

本轮补正：机械验证报告与 `planning/reviews/` 分离。应用优先读取 `planning/verifications/` 或明确命名为 `*-verification.md` 的结构化 Markdown 报告；`reviews/` 仅作为规划 Agent 在用户提出不满意后生成的反馈记录，不再被误读为验证来源。复制样本新增 `planning/verifications/017-verification.md`，Chapter 017 仍需用户分别确认章节卡和正文，验证 PASS 不会自动推进任何用户状态。验收浮层和左侧验收页现在显示实际验证报告路径及 `PASS`、警告、失败或缺失状态，并可直接打开报告；该信息仍只是证据，不会替代人工确认或触发晋升。

三 Agent 边界同步固定：世界观 Agent 维护经用户确认的 Canon 设定；规划 Agent 维护跨章节事件大纲（例如 `004-006-initial-assessment.md`）、章节卡、review 与返工记录；写文 Agent 仅生成候选正文，并在写作时同步写出 `Continuity Proposal`。该提案是写文 Agent 向规划 Agent 交接“已进入正文的本章事实”的文件；正文和补丁经用户确认并晋升后，规划 Agent 只需读取它、当前状态和 Canon，即可继续事件规划，无需重新通读正文。提案中的显式关联记录补丁仍在晋升弹窗逐项展示、由用户选择是否写入。

## 真实 Agent 测试前置（2026-08-14）

已补齐真实 Agent 首轮测试所需的三项基础设施：

- Novel Tools extension 的四个 `/novel-*` 命令会写入不可见的角色标记；运行时通过 Pi 原生 `tool_call` hook 执行 fail-closed 写入权限。世界观 Agent 只能写 `planning/world-proposals/`；规划 Agent 只能写事件纲要、章节卡、review 和返工交接；写文 Agent 只能写候选正文和 Continuity Proposal；review 模式只能写 review 和返工交接。没有角色标记、越出项目根目录、或试图写入 `canon/`、`manuscript/`、`.novel/` 等受保护路径均被阻止。小说项目内的 `bash` 也被阻止，避免通过间接命令绕过目录限制。
- 新增 `scripts/verify-novel-chapter.ts`。它先校验 chapter architecture 与章节卡的路径、编号、长度和场景预算一致，再实际读取候选正文，检查标题唯一性、正文总字符、场景数量/边界/长度、段落叙事单元、非法 HTML 注释、列表、章节卡字段泄漏、占位符/提示词残留、禁止角色和章节卡声明的字面量禁剧透，并生成 `planning/verifications/NNN-verification.md`。`target_chars` 仅作报告参考；叙事单元不足返回 `PASS_WITH_WARNINGS` 且退出码为 0，其余硬错误返回 `FAIL` 和退出码 1。写作中可使用 `--scene <scene-id>` 仅验证已完成场景，不会要求后续场景或最终长度。`PASS` 仍只是机械证据，不能替代用户验收、语义连续性判断或 Canon 晋升。
- 新增 `scripts/prepare-novel-agent-test.ps1`。它只允许向不存在或为空的目标目录复制样本，并在 `.novel/agent-test-baseline.json` 写入 SHA-256 基线；不会修改原始 `fate-control-cycle`，也不会让 Agent 直接在 `fixtures/` 中写入。

首轮 Chapter 017 测试建议先创建隔离副本：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/prepare-novel-agent-test.ps1 `
  -Destination "D:\PycharmProjects\novel_test\runtime\fate-control-cycle-agent-test"
```

将该目录作为 Desktop 当前工作区，确认章节卡后使用 `/novel-write` 生成或修改候选正文和 Continuity Proposal。写文 Agent 完成这两项工作后必须自行调用 `verify_chapter`：工具会在隔离项目内运行验证器并写入 `planning/verifications/017-verification.md`。报告为 `FAIL` 时，写文 Agent 应先依据报告返修并再次验证；仅 `PASS` 或 `PASS_WITH_WARNINGS` 才能交由用户人工阅读。用户无需手动运行机械验证命令。

验证通过后，仍须由用户人工阅读正文并在应用工作流中确认；只有该确认后的显式晋升操作可以写入 Canon。

## 章节推进与历史验收（2026-08-15）

章节验收栏采用“当前推进线 + 历史记录”双层展示：

- 当前推进章节由连续的 Canon 边界推导。存在未完成章节时，显示最早待处理章节；当前所有已有章节均已晋升后，自动生成下一章的虚拟记录，例如第 018 章。
- 虚拟章节不伪造 Markdown 文件，状态显示为“待规划”。点击后切换到对应项目的聊天运行时，并预填 `/novel-plan` 任务；提示词只携带项目内文件路径索引，不自动发送、不直接写盘。
- 规划入口至少提供上一章 Canon、上一章 Continuity Proposal、当前故事状态、连续性账本和 Canonical Text Index 等可追溯路径。规划 Agent 仍只负责事件纲要、章节卡、review 与返工交接。
- 已晋升章节不会从项目中移除，历史入口可重新调出任意已记录章节，查看关联文件、验收记录和晋升/回滚状态。回滚后重新按当前 Canon 边界推导推进线，允许回到被回退章节。
- 章节卡确认、正文人工验收和 Canon 晋升仍是三个独立关口；历史查看只改变 UI 投影，不改变文件权威性或隐式推进任何状态。

本轮补充：章节推进规则已抽为 `src/novel/workflow.ts` 的纯函数 `resolveNextWorkflowChapter`，由领域 smoke 覆盖“全部晋升后生成下一章”“回滚后回到被回退章节”和“存在更早未完成章节时优先处理缺口”三种边界。侧边栏只负责加载记录并消费该结果，不再私有维护推进算法。

本轮补充：Agent 运行结束且当前处于小说导航或章节验收模式时，桌面端会重新扫描 Novel Project，并刷新 Context Inspector。该刷新只更新文件投影、上下文索引和工作流显示，不自动接受章节卡、正文或晋升 Canon。

本轮补正：`chapter-architecture.md` 是写作前不可跳过的规划前置合同。规划 Agent 负责先登记章节编号、输出路径、章节卡路径、字数和场景预算，再创建或更新章节卡，二者共享字段必须一致，之后才可请求用户确认章节卡。写作 Agent 保持无权写入 `planning/`；章节未登记、章节卡缺失或架构/章节卡不一致时，桌面端将章节流程投影为“已阻止”并只提供“开始第 N 章规划”入口，`verify_chapter` 也会明确提示切换回规划 Agent，而不会要求写作 Agent 修复正文。规划 Agent 的受限写入范围新增 `planning/chapter-architecture.md`、`planning/progress.md` 及对应 `plan/` 兼容路径。

## 三 Agent 独立会话路由（2026-08-15）

已将世界观、规划和写作从“同一聊天中切换角色”升级为每个小说项目独立复用的三个 Pi 会话：

- `WorkspaceSessionTab` 增加 `novelRole`，旧工作区数据自动迁移；同一项目首次触发某个角色时创建专用会话标签，之后按项目和角色复用，不与普通聊天会话混用。
- 章节卡规划、章节卡 review/返工进入规划 Agent；章节卡确认后的写作、正文 review/返工进入写作 Agent；小说资源页提供世界观 Agent 入口。规划 Agent 的 review 模式不单独创建第四个 Agent。
- Desktop 在切换到目标会话并完成 runtime 加载后，才预填任务；仍不自动发送。任务带有由 Desktop 注入的 `<novel-role>` 权限标记和职责说明，现有 Novel Tools 的 fail-closed 写入权限继续生效。
- 手动输入 `/novel-world`、`/novel-plan`、`/novel-write`、`/novel-review` 也先经过 Desktop 路由到对应角色会话；命令参数作为本次任务说明保留，不再把 slash 命令直接执行在当前普通会话中。
- 角色会话之间只通过项目文件交接，不传递聊天历史。新工作流任务可以显式刷新一次文件索引上下文；同一 Agent 会话中的普通追问继续复用已有上下文，避免重复附加 `<novel-context>`。
- 角色任务提示词集中在 `src/novel/agents.ts`，领域 smoke 覆盖角色标记、章节编号、上下文路径和用户意见的拼装结果。

## 记忆系统后续设计约束（2026-08-15）

记忆系统暂不进入当前实现阶段。进入记忆系统设计阶段时，先阅读并评估腾讯云开源项目
[TencentDB-Agent-Memory](https://github.com/TencentCloud/TencentDB-Agent-Memory)，再结合本项目的 local-first 小说工作流做取舍和改造；本阶段不提前复制其代码、依赖或数据格式。

设计时重点评估以下方向：

- 将外部项目中的记忆分层、提取、检索、更新和治理思路，与本项目已有的 `CANON`、`DERIVED`、`SUMMARY`、`SCRATCH` 四层模型对照，而不是直接照搬命名或流程。
- 保持 Markdown/普通文件和 `.novel/project.json` 为小说内容的权威来源；数据库、索引或缓存只能作为可重建的派生层，不能替代文件系统，也不能绕过用户确认修改 Canon。
- 为每条记忆保留来源文件、来源片段或事件、生成者/Agent、创建时间、更新时间、生命周期/权威性和置信度，支持从记忆回溯到原文证据。
- 明确区分会话内临时记忆、项目级可复用记忆和跨章节连续性记忆；续聊、切换 Agent、切换项目时不能发生隐式串用。
- 检查记忆写入、合并、冲突检测、失效/归档和人工审核边界，优先采用可预览、可拒绝、可回滚的提案式更新。
- 评估是否需要 SQLite/LevelDB、全文索引或向量检索，但只有在 Markdown 文件 + 确定性搜索无法满足真实失败场景时才引入；不得因参考项目存在数据库就默认引入数据库或 embedding。
- 结合三 Agent 边界设计记忆权限：世界观 Agent 维护设定提案，规划 Agent 维护规划与事件状态，写文 Agent 维护本章事实/Continuity Proposal；任何进入 Canon 的记忆仍需用户确认。
- 设计 Context Builder 与记忆检索的接口，使每次注入都能显示命中记忆、来源、理由、估算 token 和是否 required，并支持用户移除本轮记忆。
- 在设计开始前先建立小规模真实样本和可审计评测：召回正确性、过期记忆污染、跨章节冲突、重复记忆合并、路径越界、项目切换隔离和重建一致性。

预期流程：

```text
阅读 TencentDB-Agent-Memory
        ↓
对照当前 Novel Project 文件/权威性模型
        ↓
确定记忆数据模型、权限和 provenance
        ↓
先实现可重建的本地索引/检索最小闭环
        ↓
加入记忆提案、审核、冲突与回滚
        ↓
用真实章节工作流验证后再考虑摘要自动化或向量检索
```

在没有完成上述设计和评测前，不新增 `memory` 自动写入工具，不让 Agent 自动把摘要、推断或会话内容提升为 Canon，也不把记忆系统作为当前主线的隐式依赖。

## 世界观 Canon 变更流（2026-08-17）

已确认的世界观文件仍可由作者直接编辑；但任何 Agent 驱动的修改都必须走独立于章节返工的“世界观变更”流程，不能将章节卡/正文的验收状态机套用于长期设定。

```text
用户选择 Canon 世界观文件并提出意见
        ↓
planning/world-proposals/ 保存不可覆盖的变更请求
        ↓
世界观 Agent 仅在该目录创建或返工完整替换提案
        ↓
用户在 UI 审阅当前 Canon、建议替换全文与声明的受影响文件
        ↓
重新核验目标指纹后，用户确认合并
        ↓
写入 Canon，并保存 .novel/world-change-history/ 快照与审计记录
```

提案必须声明 `change_id`、`target_path`、`target_fingerprint`、`affected_paths` 和唯一的 `## Proposed Canon Text` Markdown 围栏。应用不会从自然语言猜测替换内容或受影响范围；目标 Canon 或提案在审阅后发生变化时，合并 fail-closed。受影响文件仅用于透明展示和人工判断，不会被自动重写；已晋升正文也不会因为世界观变更而静默修改。

## 世界观变更历史与安全回滚（2026-09-03）

用户已反馈上一阶段真实 Agent 测试通过；本阶段补齐世界观变更的历史查看和用户确认回滚，暂不启动记忆系统。

- “小说资源 → 世界观变更”划分为“新建请求 / 待审阅 / 变更历史”，支持刷新。历史保留目标文件、原提案路径、合并时间、变更前后全文快照、声明的关联文件及回滚时间；文件路径仍可点击打开。
- 已合并和已回滚的提案均不再进入待审阅，旧请求、旧提案和历史快照不删除；再次调整需创建新的变更请求。
- 回滚先展示当前版本和将恢复的版本，只有第二次显式确认才写入。只恢复该 Canon 文件，不回退章节正文、规划或关联文件，也不自动生成新提案。
- 同一文件必须从最新的未回滚变更开始逐条撤销。执行前重新读取历史和 Canon；快照损坏、预览后历史变化、目标缺失或已有后续编辑时，拒绝覆盖。当前编辑器仍有未保存内容或正在保存时同样阻止操作。
- 新历史附带项目根目录绑定，活动项目切换后拒绝继续执行原窗口的写操作；旧版无绑定记录仍可通过结构、路径、快照指纹和当前全文校验读取。带绑定的历史不支持直接搬到另一根目录后执行回滚，后续项目迁移应另设显式确认流程。
- 成功回滚后保留前后快照并记录 `rolledBackAt`，追加 `.novel/world-change-log.md` 审计记录，同时刷新文件预览、小说导航和上下文索引。回滚只改变目标文件的内容，不改变其 Canon 身份。
- 合并/回滚按项目防止并发重复点击；对捕获到的写入异常做逆序补偿，包括失败写入已截断文件的情况。若补偿自身失败，保留可用快照并报告受影响路径。本实现不承诺断电、进程强杀或多进程同时写入下的事务原子性。
- 修复替换全文中的 `##` 标题被误当作提案分节、导致提案无法解析的问题。

验证入口：`npm run check`、`npm run test:novel-domain`、`npm run build:frontend`。新增世界观专项测试覆盖完整合并/回滚、旧版记录、重复操作、倒序回滚、后续编辑、项目绑定、损坏快照、变更后的提案和 12 个写入故障场景（其中 6 个模拟部分写入）。

界面可通过 `npm run dev` 后打开 `/scripts/world-change-preview.html` 复现，使用内存虚拟数据，不读写真实小说。已检查正常回滚、冲突禁用、空历史、新建与待审阅入口及小窗口布局；这不替代原生 Tauri 的真实文件集成验收。

## 本地记忆检索 MVP（2026-09-12）

现进入记忆系统的第一阶段，以上“暂不启动记忆系统”描述为当时的阶段边界。参考评估与取舍记录在 `docs/NOVEL_MEMORY.md`；评估固定到 TencentDB-Agent-Memory 的 `0468a2a5b50eaafc54758ed1e2e6609472e5b6ce`，未复制上游代码，也未新增数据库、embedding 或服务依赖。

- 已实现可重建的只读来源索引：CANON 设定/正文、有效人工验收绑定的候选正文，以及配置指定的当前状态/连续性记录。普通笔记、未审核摘要、原始 Continuity Proposal 不自动成为事实来源。
- 每条记忆保留项目、相对路径、源文件 SHA-256、行范围、类型、权威性、章节/时间属性及索引器版本。查询和读取时重建快照；源文修改、删除、验收失效或切换项目会使旧引用失效。精确恢复源文后允许恢复对应来源 ID。
- 上下文工作台新增“记忆检索”，提供关键词、截至章节、未来节点筛选、来源与片段预览、手动添加/移除引用。注入仍只有引用，不拼接正文；手动选择变更只刷新下一次请求，普通续聊不重复附加上下文。
- 三职能共享项目内只读工具 `search_story_memory` / `read_story_memory`，通过 managed Novel Tools extension v6 发布。需重启 Desktop/对应 Pi runtime 加载新版本；不增加自动写入或晋升权限。
- 本轮自动验证通过：`npm run check`、`npm run test:novel-domain`、`npm run build:frontend`。领域测试包含原位修改、删除、验收变化、回滚身份恢复、项目隔离、时间筛选、预算及路径边界；Pi 实际扩展加载器已验证新工具注册与调用。四组真实只读 fixture 检索均在 Top 8 中找到预期来源（4/4），仅为小样本基线，不代表语义理解准确率。
- 尚待完成：生产压缩后扩展工厂的独立加载验证、记忆工作台完整交互/小窗口验收，以及原生 Tauri 文件合并/回滚集成验收。原生测试窗口已启动，但用户按 Esc 停止了桌面控制，未执行初始化或合并/回滚，不记为通过。

隔离测试入口：`/scripts/memory-preview.html` 使用虚拟内存数据；`scripts/tauri-memory-test.json` 用于原生测试窗口，只有点击初始化后才在系统临时目录创建独立副本，不使用真实小说项目。正常启动仍使用 `npm run tauri -- dev`，不要直接运行可能保留测试配置的旧 debug 可执行文件。
