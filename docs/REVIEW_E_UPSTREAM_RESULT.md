# E8 沿用上游预算：执行结果

2026-09-25。用户明确要求暂不继续研究代理预算兼容，直接使用现有上游设置。本轮据此创建独立 `joint-upstream` 批次并执行，不复用任何历史授权；没有修改全局 Pi / 代理设置或生产代码。

## 本轮边界

- 沿用已观察到的上游 65,536 输出 / 16,384 思考设置。本地保守按两者之和 **81,920** 预留；这是固定 SDK 计量口径下的安全余量，不宣称供应商一定分开计费或客户端上限已经生效。
- 原生请求参数保持原样（普通请求 2,048，摘要由 SDK 使用更小值），收据同时记录真实客户端字段和上游预算预留，不能混淆二者。未关闭 reasoning 计量。
- 仍最多全批 **24** / 每任务 **8** 次请求，原超时、输入 / 响应体积、工具权限、未知用量及零重放保护不变。满额无缓存预留参考 **$8.552448**，已在执行前说明；不等于账单或美元硬限额。
- 使用上一轮完成的枚举评分契约 v2；旧 profile、计划、题目、成绩和证据不覆盖。

清单：`artifacts/harness/review-e8/joint-upstream-gYx27t/manifest.json`。

SHA-256：`c6f071d650f6b4d04ad0606cf4d1e48ea3ad2b1a33f1de37fcffd73ce81ba780`。准备 / 执行前 verify 通过；授权于 19:03:33.546 UTC+8 消耗，执行后 verify 仍通过并显示 `approvalConsumed=true`。不能继续使用剩余额度。

## 实测结果：U / C 完成，R 末尾被本地工具结果预算阻断

共 **14 次派发、14 条完整响应、14 条 input / output 配对**；其中包含 **2 次原生摘要**。无输出用量超额，无重试。整体退出 **1**，不能称为整套联合验收通过。

| 场景 | 本次结果 |
| --- | --- |
| U 用量配对 | 2 次请求完成，原始 usage 与 SDK 计量对应 |
| C 未压缩对照 | v2 严格评分 10/10，关键项 6/6 |
| C 压缩后恢复 | 2 次原生摘要 + 回答完成，10/10，关键项 6/6，原 seed 保留 |
| R 读取与新鲜度 | 同上下文复用、下一上下文重新核验、宿主 v1→v2 变更、实际 v2 重读均已观察；末尾回答未完成 |

R 执行了 8 个允许的工具调用。最后一次 `refresh_task_checkpoint` / `get_context_budget` 工具结果已经产生，但继续生成回答时，生产扩展的 `boundToolOutput` 报 **OUTPUT_BUDGET_EXCEEDED：本轮工具结果累计预算已用尽**。broker 最终记 `worker_failed`，`readEvidence.finalAnswerReportsV2=false`。

这是**本地累计工具结果体积保护**，不同于上游生成 token 的 2K 预留问题。R 的持久台账显示 8 次实际派发、另有 1 次在上下文阶段取消的 SDK 入口；后者不是第 15 次 HTTP。没有为取得通过而放宽该保护或补发请求。其累计 / 重投影和诊断结果体积是否合理尚待单独诊断，不直接断言计数存在 bug。

本轮累计输入 **82,973**、completion **1,525**、reasoning **7,682**，SDK 输出 **9,207**。缓存均未知，参考 / 实际费用均保持 **null**。本批单次 SDK 输出最高 1,303，因而没有真实触及旧的 2,048 阈值；允许 9,109 输出继续的行为由独立合成回归覆盖，不冒充本批实测观察。兼容校准锚点仍未命中，不修改估算器。

两个 C 答案通过不等于“摘要单独恢复全部信息”或完整摘要质量验收。助手只读查看了摘要：只读、待人工批准、拒绝跳过来源检查、v2、按需刷新和写作职能无 planning 权限均有保留；“正文尚未机械验证”未被同样明确地单列。答案还使用生产 TaskContract / checkpoint 与近期历史，`summaryContentReview` 继续保留 `pending_review`。

## 验证与保全

- 新定向 **9** 组通过：`upstream-tests-lEaAFZ`；旧枚举 / 参数 **16** 组：`budget-contract-tests-Mk9stK`；旧联合 **16** 组：`joint-tool-none-tests-97j2Xl`。均在 `artifacts/harness/review-e8/` 下，共 **41** 组，另有应用 / 测试类型检查和 diff 检查通过。
- 首轮新测试 **8/9** 保留于 `upstream-tests-GnZerK`：测试误将原生摘要输出参数也假定为 2,048，实际 SDK 为 reserveTokens 的 0.8 / 0.5 倍；仅修正测试，没有改摘要生成器。
- 原报告：`artifacts/harness/review-e8/joint-upstream-gYx27t/live/report.json`。recover 校验哈希链后为 14 预留 / 14 终态 / 0 未结 / 0 新请求，禁止重放。
- 三个历史批次 **8 + 8 + 10** 项证据 SHA 不变；本轮 manifest 中 140 项生产 / 冻结评测 / 历史题目文件未变；四个隔离项目的 Canon / 验收哨兵不变，只有 R 来源按设计改为 v2。
- 没有真实小说、Desktop、Rust、远程 CI、commit / push 操作。

下一步不再研究代理参数映射。应先诊断 R 中累计工具结果体积（特别是预算 / 检查点诊断和历史结果重投影），再用独立新批次补测未完成部分；不重跑已完成的 U / C，不复用本次授权。

后续：已确认并修复诊断工具结果的重复计量，独立 R 补测通过；见 [R 补测与修复记录](REVIEW_E_READ_RETEST_RESULT.md)。以上仍保留为旧批次当时的结果，不回填旧报告或分数。

后续摘要逐项审读见 [审读记录](REVIEW_E_SUMMARY_CONTENT_REVIEW.md)：正文未验证状态未被明确保留；实际答题请求使用检查点原话，原生摘要正文并未进入请求。旧 `pending_review` 作为运行时记录保持原样，后续独立审读结论为 `completed_with_gap`，不代替用户验收。
